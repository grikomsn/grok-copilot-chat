---
"grok-copilot-chat": patch
---

Harden the browser OAuth callback against forged requests and reflected markup with state-first validation, strict loopback request checks, non-reflective error pages, output encoding, and restrictive browser response headers.
