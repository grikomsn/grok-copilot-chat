---
"grok-copilot-chat": patch
---

Fix VS Code context-window accounting, track exact xAI-billed spend and request tokens locally, and relabel response-header values as transient API rate capacity rather than account usage. Remove the inaccessible Grok-web limit probe and consolidate shared provider helpers.
